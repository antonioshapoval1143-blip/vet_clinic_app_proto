# vet_clinic_app_proto

The prototype of veterinary clinic app, created with Qt as frontend, Python as backend, MySQL database to collect all data

# how to use

first of, you need to have vet_clinic mysql database with 9 tables. if you have one, then start "dbui.py". 
